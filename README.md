# 🧠 Mini Nginx Server with Node.js

A simple static file server built using Node.js `http` and `fs` modules.

### Features
- Serves HTML, CSS, JS, and images
- Handles 404 & server errors
- No external libraries

### Run
```bash
node server.js
